<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Memo</title>  <!-- 페이지 제목 -->
  </head>
  <body>
    <h1>Edit Memo</h1>  <!-- "메모 수정" 제목 -->

    <!-- 메모 수정 폼 -->
    <form action="/memos/edit/<%= memo.id %>" method="POST">  <!-- 수정할 메모의 ID를 포함한 POST 폼 -->
      <label for="title">Title:</label>
      <input
        type="text"
        id="title"
        name="title"
        value="<%= memo.title %>"  <!-- 기존 메모 제목을 미리 채워놓기 -->
        required />
      <br />
      <label for="content">Content:</label>
      <textarea id="content" name="content" required>  <!-- 기존 메모 내용 미리 채우기 -->
<%= memo.content %></textarea>
      <br />
      <button type="submit">Update Memo</button>  <!-- 메모 수정 버튼 -->
    </form>
  </body>
</html>
